# Evolution - P3 phase boundary (2026-09-02)

Window: trajectory steps 15-17.

## Step 1 - Diagnosis

**Step 16, "re_derived_procedure":** `tests/test_api_companies.py` (P1),
`tests/test_versioning.py` (P1), and `tests/test_rule_engine.py` (P2) each
independently created their own `create_engine(...)` + `sessionmaker(...)`
pointed at the test database. None of them referenced each other; each was
written as if it were the first test file needing direct DB access. Caught
only while writing P3's tests (a fourth file about to do the same thing) -
not by a test failure, by noticing the duplication. Component: `K` (the
existing `add-api-endpoint.md` skill covered *how to add an endpoint* but
had nothing about *how to write its tests* beyond "use the client fixture,"
which didn't exist yet when the first three files were written).

No other signature: the P3 service logic (override predicate, resolve/
health-check/outcome) and its 11 new tests passed first try; live-container
verification of the exact §10 acceptance criterion (422 without a note, 200
with one) matched on the first attempt; nothing was re-litigated; scope
stayed inside P3 (outcome-closing was flagged in STATE.md ahead of time as
needed by P3 even though §10 doesn't list it there, not a scope drift); no
constitution rule was tested.

## Step 2 - Four passes

**Pass 1 (prompt p): no edit.** This is specific enough to belong in the
skill file, not general prompt guidance.

**Pass 2 (sub-agents G): no edit.** Four phases done linearly and cleanly.

**Pass 3 (skills K): UPDATE `harness/skills/add-api-endpoint.md`.** Added
point 7: service-level tests import `tests.conftest.TestSession`, never
redefine an engine. Also tightened point 6 to name the `client` fixture
explicitly (it existed by P2 but the skill still described the pattern
generically). Also consolidated the three duplicate engines into
`tests/conftest.TestSession` as part of this window's application-code
commit (6fe172b), so the fix and the codification landed together.

**Pass 4 (memory M): no edit beyond STATE.md.** ADR-010/011 were already
written with citations during P3 itself.

## Step 3 - Metrics snapshot

| Metric | Value |
|---|---|
| Acceptance criteria passing / total (P3) | 1/1 - override-without-note refused (422), with-note succeeds with override=TRUE, verified live |
| Test pass rate | 46/46 |
| Tests added this window | 11 |
| Rework rate (files edited 3+ times) | 0 in this window (the 3x duplication happened *across* P1/P2/P3 windows, caught and fixed within this one) |
| Redundant reads | 0 |
| Re-derivations | 1 (see diagnosis) - now closed by the skill update |
| Open questions raised vs resolved | 0 / 0 |
| Harness size | `prompt.md` 76 lines; `G` 0; `K` 2 (1 updated); `knowledge/` 1; `M` 11 ADRs + 3 gotchas |

Four windows in (P0-P3), one real signature total, caught and closed the
same cycle it was noticed. No convergence signal to stop refining - still
finding small, genuine things to fix, not manufacturing churn.
