# Evolution - P5 phase boundary (2026-09-03)

Window: trajectory steps 21-24.

## Step 1 - Diagnosis

**Step 23, "environment_surprise":** `Dockerfile` didn't `COPY` the new
`frontend/` or `contracts/` directories, so the P5 `StaticFiles` mounts
404'd on the first rebuild. Caught immediately by the routine per-phase
live-container curl checks, fixed same step. Component: `M` (gotchas.md) -
logged as a reminder for P6, which may face the same class of omission if
`eval/` needs to be present at runtime.

**Step 24, human-originated, not a failure:** two files appeared in the
repo mid-session that this session didn't create - a `TO DO` note (new
feature requests: GitHub push, multi-user auth/RBAC, UX polish) and a
`.production.env` with a live database credential. Neither is a harness
failure signature; both are logged to `open-questions.md` per the
constitution ("write to open-questions.md and stop" when something needs
the human) rather than acted on. `.gitignore` hardened (`*.env`) as a
direct, low-risk response - the credential was never staged, but the
pattern gap that could have let a similarly-named file slip through in a
careless `git add -A` is now closed regardless of this specific file.

No other signature this window: the backend expansion (ADR-013) and the
full frontend build passed their real-browser verification without a
second failure; nothing was re-derived (the skill/knowledge base held); no
constitution rule was tested.

## Step 2 - Four passes

**Pass 1 (prompt p): no edit.** Neither finding is general behavioral
guidance; the Docker-copy fact belongs in gotchas.md and the open questions
are one-off content, not a process rule.

**Pass 2 (sub-agents G): no edit.**

**Pass 3 (skills K): no edit.** The Dockerfile omission happened once, not
3+ times with a stable shape - below the CREATE bar. `add-api-endpoint.md`
wasn't directly exercised this window (P5 is frontend-only) but nothing
contradicted it either.

**Pass 4 (memory M): CREATE two `open-questions.md` entries** (TODO note,
exposed credential) and **one `gotchas.md` entry** (Dockerfile COPY list).
`STATE.md` rewritten below - its "Blocked" section now surfaces the open
questions prominently, since continuing straight to P6 without the human
seeing these would be exactly the kind of silent scope-drift the
constitution's rule 1 exists to prevent.

## Step 3 - Metrics snapshot

| Metric | Value |
|---|---|
| Acceptance criteria passing / total (P5) | 1/1 - dynamic metrics field proven live via a real `metric_definitions` insert, zero frontend edits |
| Test pass rate | 55/55 (backend; no automated suite covers static frontend - browser trace is the P5 verification) |
| Tests added this window | 1 (`test_company_detail_includes_observations_triggers_and_override_flag`) + 1 (`test_company_card_list_includes_core_metrics_snapshot`) |
| Rework rate | 0 |
| Redundant reads / re-derivations | 0 |
| Open questions raised vs resolved | 2 raised (this window) / 0 resolved - both need the human |
| Harness size | `prompt.md` 76 lines; `G` 0; `K` 2; `knowledge/` 1; `M` 14 ADRs + 4 gotchas + 2 open questions |

First window with unresolved open questions outstanding. Per prompt.md's
hard rules: "If blocked on something only the human can answer, write it to
open-questions.md and move to unblocked work" - P6 is unblocked work (it
doesn't depend on the TODO items or the credential), so it's reasonable to
continue, but the human needs to see these before this session goes further
on the TODO's implied scope (GitHub, multi-user auth) or touches the
production database.
