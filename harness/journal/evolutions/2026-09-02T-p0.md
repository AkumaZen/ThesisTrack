# Evolution - P0 phase boundary (2026-09-02)

Window: trajectory steps 1–7 (H_min bootstrap through P0 verification).

## Step 1 - Diagnosis

One failure signature in the window:

- **Step 3, "environment_surprise"**: host port 5432 was already bound by an
  unrelated project's Postgres container (`restaurantapp-db-1`). Docker
  compose started the service anyway without publishing the port, and a
  client connecting to `localhost:5432` silently reached the *other*
  project's database, producing a misleading `password authentication
  failed` error rather than `connection refused`. Component at fault: `M`
  (missing gotcha) - this is a fact about this machine's environment, not a
  behavioral gap in the prompt or a missing procedure.

No other signature in the table (harness/prompt.md's failure-signature list)
appeared. Nothing was re-read 3+ times, nothing was re-derived, no decision
was re-litigated (nothing existed yet to re-litigate), no test-fix-test loop
ran more than once, no API/column/flag was invented without basis (the one
genuine interpretive step - adapting the user-supplied Balu Forge sample to
BUILD_PLAN.md's actual contract shape - was clarified with the human first
and logged as three cited ADRs, not guessed), scope stayed inside P0, and no
constitution rule was tested.

## Step 2 - Four passes

**Pass 1 (prompt p): no edit.** The port-collision fact is machine-specific
trivia, not a generalizable behavioral rule; it belongs in `gotchas.md`
(already written, inline, at the time it was hit - see
`harness/memory/gotchas.md`), not in the prompt.

**Pass 2 (sub-agents G): no edit.** Nothing in this window recurred 3+ times
with a stable shape. First pass through the harness - there's no history yet
to justify a role split.

**Pass 3 (skills K): CREATE `harness/skills/bin/evolve.sh`.** Assembling
context for a Refiner pass (trajectory window, harness file listing, commits
since the last evolution, test status, prompt line count) is a sequence that
just succeeded once by hand in this pass and will clearly recur at every
future phase boundary - the CREATE bar in refiner.md Pass 3 ("codifying a
sequence that succeeded and will recur; prefer an executable script"). Ran it
once against this exact window as a sanity check (output matches the
trajectory log and current git log 1:1).

**Pass 4 (memory M): no separate edit beyond what was already written
in-line during P0** (`gotchas.md` port-collision entry; `decisions.md`
ADR-001..004 for the fixture adaptation, the `why_we_believe_it` shape
decision, the health_check-pillar-vs-table distinction, and the raw-SQL
migration choice - all already cited to file paths and passing tests at the
time they were written, per constitution rule 7). `STATE.md` is rewritten
below from actual repo state.

## Step 3 - Metrics snapshot

| Metric | Value |
|---|---|
| Acceptance criteria passing / total (P0) | 2/2 - sample payload validates clean; UPDATE thesis_versions raises |
| Test pass rate | 11/11 |
| Tests added this window | 11 (all of them - first window) |
| Rework rate (files edited 3+ times) | 0 |
| Redundant reads (same file, same fact, 3+ times) | 0 |
| Re-derivations (skill already covered it) | 0 - no skills existed yet |
| Tokens per criterion completed | not instrumented this cycle |
| Open questions raised vs resolved | 1 raised (original spec doc) → 1 resolved (user supplied it) |
| Harness size | `prompt.md` 76 lines (cap 120); `G` 0; `K` 1 (`evolve.sh`); `M` 4 ADRs + 1 gotcha + STATE.md |

No degradation to compare against - this is the first cycle.
