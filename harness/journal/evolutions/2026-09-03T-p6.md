# Evolution - P6 phase boundary (2026-09-03)

Window: trajectory steps 25-29. This closes out BUILD_PLAN.md's full P0-P6
phase set.

## Step 1 - Diagnosis

**Step 26, "environment_surprise":** after a Docker Desktop restart,
`docker compose up` failed to bind the Postgres host port with a
permissions-style error even though nothing was listening on it. Cause:
`netsh interface ipv4 show excludedportrange protocol=tcp` showed
Hyper-V/WSL2 had dynamically reserved a range that happened to include the
port this time. Moved to a port outside all current ranges. Already logged
to gotchas.md when it happened (before this evolution pass), with a note
that the symptom looks identical to the earlier "another project already
owns this port" gotcha but has a different cause and fix.

**Step 28, single-iteration test bug:** first `run_eval()` integration
test read 0 eval rows because the test company's outcome was never closed
(`include_open=False` by default correctly excluded it) - fixed in the
test itself, not the implementation. Not a signature: one round, no
re-derivation, the code was right and the test setup was wrong.

**Meta-observation, not a project-harness signature:** this session had
been writing em dashes throughout comments and harness memory files despite
the user's global CLAUDE.md already prohibiting them, until the user
explicitly caught it during the GitHub push request (step 25). This is a
standing global-instruction compliance lapse, not a gap in this project's
`harness/prompt.md` - the fix (a saved global user memory on commit-message
style, plus a one-time sweep of this repo's non-immutable files) belongs
outside this project's harness state, so no `p`/`G`/`K`/`M` edit was made
for it here beyond noting it in this record for the human reviewing evolution
history.

No other signature: the P6 build itself (migration, exporter, eval harness,
router) passed real-browser and live-container verification without
rework; nothing was re-derived from an existing skill; scope stayed inside
what STATE.md queued at the P5 boundary plus the one legitimate P3 gap
(`HealthCheck.reasoning_chain`) closed the same way P1's `GET
/companies/{id}` gap was closed at the P5 boundary - a now-consistent
pattern of "close a real gap in an earlier phase when a later phase's tests
reveal it actually mattered," not scope creep.

## Step 2 - Four passes

**Pass 1 (prompt p): no edit.** Nothing here is generalizable behavioral
guidance for this project specifically.

**Pass 2 (sub-agents G): no edit.** All seven phases (P0-P6) were built
linearly by one continuous thread with zero rework-rate increase across the
whole run. Retrospectively, this is real evidence *against* needing
role-specialized sub-agents for a project at this scale/pace - noted here
rather than acted on, since there's no more phase work to test the
alternative against.

**Pass 3 (skills K): no edit.** `add-api-endpoint.md` was followed
correctly for the export router; no new 3x-recurring pattern emerged in
this final window.

**Pass 4 (memory M): no edit beyond STATE.md**, which is rewritten below to
reflect that BUILD_PLAN.md's full v1 scope is now complete, and to hand off
cleanly to the TODO-driven follow-on work (multi-user auth, GitHub/CI
concerns, UX polish) that is explicitly outside BUILD_PLAN.md and therefore
outside this harness's original mandate - flagged as its own thing rather
than silently folded into "more phases."

## Step 3 - Metrics snapshot (P0-P6 cumulative)

| Metric | Value |
|---|---|
| Acceptance criteria passing / total | 7/7 phases, each verified at both the automated-test and live-container level |
| Test pass rate | 67/67 |
| Total tests written across the build | 67 |
| Rework rate | Low throughout - the two gap-closing passes (P1->P5, P3->P6) were the only cases of returning to touch an earlier phase's code, and both were deliberate, cited, and verified, not corrections of mistakes |
| Redundant reads / re-derivations | 1 total across the whole build (the P1 evolve cycle's DB-engine duplication finding) |
| Open questions raised vs resolved | 2 raised (P5 boundary: TODO file, credential) - both since addressed by direct user instruction outside the harness's own resolution mechanism |
| Harness size | `prompt.md` 76 lines (never grew past H_min's original 76 - no edit was ever needed); `G` 0; `K` 2; `knowledge/` 1; `M` 15 ADRs + 5 gotchas |

Seven evolve cycles, one real skill CREATE, one skill UPDATE, one
knowledge-doc CREATE, zero prompt edits, zero sub-agent CREATEs. The
prompt's failure-signature table was sufficient to catch everything that
happened without ever needing to grow itself - the strongest evidence this
build has produced that H_min was already well-calibrated for this
model/task pairing, not that refinement stopped mattering.
